@using Folly.Constants
@using Folly.Extensions
@using Folly.Resources;
@inject Microsoft​.AspNetCore​.Http.IHttpContextAccessor _HttpContextAccessor

<content>
    <breadcrumb>
        <breadcrumb-item label="@Core.Help" controller="Account" action="ToggleContextHelp" active="true" />
    </breadcrumb>

    <partial name="_Alerts" />

    <partial name="_Alert" model="new Alert { Content = _HttpContextAccessor.HttpContext?.Session.IsEnabled(SessionProperties.Help) == true ? Core.HelpEnabled : Core.HelpDisabled, CanDismiss = false }" />
</content>
